using UnityEngine;

namespace pastehack.menu.windows.main.tabs
{
	public class aimbot(Rect rect, int id , bool menu_state) : window(rect, id, nameof(aimbot), menu_state)
	{
		protected override void content()
		{
			
		}
	}
}
